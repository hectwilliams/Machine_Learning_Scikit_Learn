'''

    Q.

      Can an SVM classifier output a confidence score when it classifies an instance? What about a probability?

    A.

      The distance between sample and boundary can be measured (i.e. score). SVM classifiers do not output probabilities for each class. 

'''